[advisories]
ignore = [
    # RUSTSEC-2025-0141: bincode 1.3.3 "unmaintained" — not a security issue.
    # Pulled in via iai-callgrind (dev-only Callgrind benchmarks).
    # The bincode team considers 1.3.3 complete; no upgrade path exists upstream.
    "RUSTSEC-2025-0141",
]
